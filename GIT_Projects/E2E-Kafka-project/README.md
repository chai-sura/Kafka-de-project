# E2E Kafka-Airflow Streaming Pipeline

This project demonstrates an end-to-end data streaming pipeline using **Apache Kafka** and **Apache Airflow** with Docker Compose. The DAG fetches fake user data from an external API and streams it to a Kafka topic.


